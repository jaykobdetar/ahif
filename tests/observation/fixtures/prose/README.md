# Fictional field-observation fixtures

All rows are invented. Exact observed CSV headers are used. The first two comment bodies have independently enumerated parser results in `expected.json`: 12 and 14 retained words. The third checks a string ID above the JavaScript safe-integer limit, decomposed Unicode and CRLF fidelity; its four words are Café, stays, Unicode and remains after NFC/LF normalization. The fox emoji is not a word. The post has a supplied title, a native destination URL and an empty CSV body, which is unknown/unavailable body evidence, not proof of an originally empty contribution.

Creation times are exact, date-only and absent. Comment parent `absent7` is intentionally absent. All source language, editing, current visibility, observation time and body completeness remain unknown.
